import React from "react";
import { Overlay, Container } from "./Spinner.styles";

const Spinner = () => {
  return (
    <Overlay>
      <Container />
    </Overlay>
  );
};

export default Spinner;
