import styled from "styled-components/macro";

import { Input, Group } from "components/FormInput/FormInput.styles";

export const Container = styled.div`
  max-width: 500px;
  margin: 0 auto;
  ${Group} {
    margin: 0;
  }
`;
